import type fr from '../fr/compositionModal'
import type { Structure } from '../../types'

/** Italien — espace « compositionModal » (backlog § BL.2), traduit depuis le français. */
const compositionModal: Structure<typeof fr> = {
  fermer: "Chiudi",
  aucuneLigneNeComposeCette: "Nessuna riga compone questa categoria.",
  valeurTotale: "Valore totale:",
}

export default compositionModal
