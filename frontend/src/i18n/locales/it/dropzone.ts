import type fr from '../fr/dropzone'
import type { Structure } from '../../types'

/** Italien — espace « dropzone » (backlog § BL.2), traduit depuis le français. */
const dropzone: Structure<typeof fr> = {
  glissezUnFichierIciOu: "Trascini un file qui o clicchi per sfogliare",
  lectureDuFichier: "Lettura del file...",
  deposezLeFichierIci: "Rilasci il file qui",
}

export default dropzone
