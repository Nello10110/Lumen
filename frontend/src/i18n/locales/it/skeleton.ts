import type fr from '../fr/skeleton'
import type { Structure } from '../../types'

/** Italien — espace « skeleton » (backlog § BL.2), traduit depuis le français. */
const skeleton: Structure<typeof fr> = {
  chargementEnCours: "Caricamento in corso",
}

export default skeleton
