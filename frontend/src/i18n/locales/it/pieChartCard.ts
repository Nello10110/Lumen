import type fr from '../fr/pieChartCard'
import type { Structure } from '../../types'

/** Italien — espace « pieChartCard » (backlog § BL.2), traduit depuis le français. */
const pieChartCard: Structure<typeof fr> = {
  titreUniquePasDeDecomposition: "Titolo singolo, nessuna scomposizione interna.",
}

export default pieChartCard
