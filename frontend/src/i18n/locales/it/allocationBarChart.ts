import type fr from '../fr/allocationBarChart'
import type { Structure } from '../../types'

/** Italien — espace « allocationBarChart » (backlog § BL.2), traduit depuis le français. */
const allocationBarChart: Structure<typeof fr> = {
  voirDetail: "Vedi il dettaglio delle righe di «{categorie}»",
}

export default allocationBarChart
