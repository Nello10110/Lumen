import type fr from '../fr/lumenFaitAmusant'
import type { Structure } from '../../types'

/** Italien — espace « lumenFaitAmusant » (backlog § BL.2), traduit depuis le français. */
const lumenFaitAmusant: Structure<typeof fr> = {
  unLumenCEstLe: "Un lumen è il flusso luminoso di una candela a un metro. Il Suo patrimonio non ha un’unità SI, ma il nome lo teniamo.",
  fermer: "Chiudi",
}

export default lumenFaitAmusant
