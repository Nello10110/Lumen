import type fr from '../fr/importPage'
import type { Structure } from '../../types'

/** Italien — espace « importPage » (backlog § BL.2), traduit depuis le français. */
const importPage: Structure<typeof fr> = {
  importer: "Importa",
  choisisLaSourceDeTes: "Scegli la fonte dei tuoi dati, poi trascina il suo file di esportazione sul riquadro corrispondente.",
}

export default importPage
