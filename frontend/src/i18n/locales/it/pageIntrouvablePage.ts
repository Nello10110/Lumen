import type fr from '../fr/pageIntrouvablePage'
import type { Structure } from '../../types'

/** Italien — espace « pageIntrouvablePage » (backlog § BL.2), traduit depuis le français. */
const pageIntrouvablePage: Structure<typeof fr> = {
  aucuneLumiereParIci: "Nessuna luce da queste parti.",
  cettePageNExistePas: "Questa pagina non esiste.",
  retourAuTableauDeBord: "Torna al pannello",
}

export default pageIntrouvablePage
