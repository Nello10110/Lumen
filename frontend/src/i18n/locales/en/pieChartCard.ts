import type fr from '../fr/pieChartCard'
import type { Structure } from '../../types'

/** Anglais — espace « pieChartCard » (backlog § BL.2), traduit depuis le français. */
const pieChartCard: Structure<typeof fr> = {
  titreUniquePasDeDecomposition: "Single security, no internal breakdown.",
}

export default pieChartCard
