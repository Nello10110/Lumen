import type fr from '../fr/compteDetailPage'
import type { Structure } from '../../types'

/** Anglais — espace « compteDetailPage » (backlog § BL.2), traduit depuis le français. */
const compteDetailPage: Structure<typeof fr> = {
  retourAuxComptes: "Back to accounts",
}

export default compteDetailPage
