import type fr from '../fr/ajoutDetenteurModale'
import type { Structure } from '../../types'

/** Anglais — espace « ajoutDetenteurModale » (backlog § BL.2), traduit depuis le français. */
const ajoutDetenteurModale: Structure<typeof fr> = {
  nouvellePersonneDuFoyer: "New household member",
  nom: "Name",
  exJulie: "e.g. Julie",
  annuler: "Cancel",
  ajout: "Adding…",
  ajouter: "Add",
}

export default ajoutDetenteurModale
