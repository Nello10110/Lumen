import type fr from '../fr/compositionModal'
import type { Structure } from '../../types'

/** Anglais — espace « compositionModal » (backlog § BL.2), traduit depuis le français. */
const compositionModal: Structure<typeof fr> = {
  fermer: "Close",
  aucuneLigneNeComposeCette: "No line makes up this category.",
  valeurTotale: "Total value:",
}

export default compositionModal
