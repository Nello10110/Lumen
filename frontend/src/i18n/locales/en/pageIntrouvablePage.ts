import type fr from '../fr/pageIntrouvablePage'
import type { Structure } from '../../types'

/** Anglais — espace « pageIntrouvablePage » (backlog § BL.2), traduit depuis le français. */
const pageIntrouvablePage: Structure<typeof fr> = {
  aucuneLumiereParIci: "No light around here.",
  cettePageNExistePas: "This page does not exist.",
  retourAuTableauDeBord: "Back to the dashboard",
}

export default pageIntrouvablePage
