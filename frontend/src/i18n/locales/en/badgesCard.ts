import type fr from '../fr/badgesCard'
import type { Structure } from '../../types'

/** Anglais — espace « badgesCard » (backlog § BL.2), traduit depuis le français. */
const badgesCard: Structure<typeof fr> = {
  badges: "Badges",
  unePetiteGalerieStrictementPersonnelle: "A small, strictly personal gallery — never shared, never compared. It rewards regular tracking, never the amount invested.",
  obtenuLe: "Earned on",
  pasEncoreObtenu: "Not earned yet",
}

export default badgesCard
