import type fr from '../fr/labelAdaptatif'
import type { Structure } from '../../types'

/** Anglais — espace « labelAdaptatif » (backlog § BL.2), traduit depuis le français. */
const labelAdaptatif: Structure<typeof fr> = {
  langageSimple: "plain language",
  termeTechnique: "technical term",
}

export default labelAdaptatif
