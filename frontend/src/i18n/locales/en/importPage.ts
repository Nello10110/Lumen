import type fr from '../fr/importPage'
import type { Structure } from '../../types'

/** Anglais — espace « importPage » (backlog § BL.2), traduit depuis le français. */
const importPage: Structure<typeof fr> = {
  importer: "Import",
  choisisLaSourceDeTes: "Choose the source of your data, then drop its export file on the matching tile.",
}

export default importPage
