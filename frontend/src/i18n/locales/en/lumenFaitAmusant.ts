import type fr from '../fr/lumenFaitAmusant'
import type { Structure } from '../../types'

/** Anglais — espace « lumenFaitAmusant » (backlog § BL.2), traduit depuis le français. */
const lumenFaitAmusant: Structure<typeof fr> = {
  unLumenCEstLe: "A lumen is the luminous flux of a candle at one meter. Your wealth has no SI unit — but we keep the name.",
  fermer: "Close",
}

export default lumenFaitAmusant
