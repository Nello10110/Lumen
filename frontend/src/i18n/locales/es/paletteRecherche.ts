import type fr from '../fr/paletteRecherche'
import type { Structure } from '../../types'

/** Espagnol — espace « paletteRecherche » (backlog § BL.2), traduit depuis le français. */
const paletteRecherche: Structure<typeof fr> = {
  recherche: "Búsqueda",
}

export default paletteRecherche
