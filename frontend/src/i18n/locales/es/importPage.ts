import type fr from '../fr/importPage'
import type { Structure } from '../../types'

/** Espagnol — espace « importPage » (backlog § BL.2), traduit depuis le français. */
const importPage: Structure<typeof fr> = {
  importer: "Importar",
  choisisLaSourceDeTes: "Elige la fuente de tus datos y luego suelta su archivo de exportación en la ficha correspondiente.",
}

export default importPage
