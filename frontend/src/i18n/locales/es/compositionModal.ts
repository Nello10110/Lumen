import type fr from '../fr/compositionModal'
import type { Structure } from '../../types'

/** Espagnol — espace « compositionModal » (backlog § BL.2), traduit depuis le français. */
const compositionModal: Structure<typeof fr> = {
  fermer: "Cerrar",
  aucuneLigneNeComposeCette: "Ninguna línea compone esta categoría.",
  valeurTotale: "Valor total:",
}

export default compositionModal
