import type fr from '../fr/compteDetailModal'
import type { Structure } from '../../types'

/** Espagnol — espace « compteDetailModal » (backlog § BL.2), traduit depuis le français. */
const compteDetailModal: Structure<typeof fr> = {
  compte: "Cuenta",
  fermer: "Cerrar",
  ouvrirEnPleinePage: "Abrir en página completa",
}

export default compteDetailModal
