import type fr from '../fr/lumenFaitAmusant'
import type { Structure } from '../../types'

/** Espagnol — espace « lumenFaitAmusant » (backlog § BL.2), traduit depuis le français. */
const lumenFaitAmusant: Structure<typeof fr> = {
  unLumenCEstLe: "Un lumen es el flujo luminoso de una vela a un metro. Su patrimonio no tiene unidad del SI, pero conservamos el nombre.",
  fermer: "Cerrar",
}

export default lumenFaitAmusant
