import type fr from '../fr/pageIntrouvablePage'
import type { Structure } from '../../types'

/** Espagnol — espace « pageIntrouvablePage » (backlog § BL.2), traduit depuis le français. */
const pageIntrouvablePage: Structure<typeof fr> = {
  aucuneLumiereParIci: "Aquí no hay luz.",
  cettePageNExistePas: "Esta página no existe.",
  retourAuTableauDeBord: "Volver al panel",
}

export default pageIntrouvablePage
