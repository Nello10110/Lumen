import type fr from '../fr/field'
import type { Structure } from '../../types'

/** Allemand — espace « field » (backlog § BL.2), traduit depuis le français. */
const field: Structure<typeof fr> = {
  fermer: "Schließen",
}

export default field
