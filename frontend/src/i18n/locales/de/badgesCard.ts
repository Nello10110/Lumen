import type fr from '../fr/badgesCard'
import type { Structure } from '../../types'

/** Allemand — espace « badgesCard » (backlog § BL.2), traduit depuis le français. */
const badgesCard: Structure<typeof fr> = {
  badges: "Abzeichen",
  unePetiteGalerieStrictementPersonnelle: "Eine kleine, streng persönliche Galerie — nie geteilt, nie verglichen. Sie würdigt die Regelmäßigkeit der Pflege, nie den investierten Betrag.",
  obtenuLe: "Erhalten am",
  pasEncoreObtenu: "Noch nicht erhalten",
}

export default badgesCard
