import type fr from '../fr/importPage'
import type { Structure } from '../../types'

/** Allemand — espace « importPage » (backlog § BL.2), traduit depuis le français. */
const importPage: Structure<typeof fr> = {
  importer: "Importieren",
  choisisLaSourceDeTes: "Wähle die Quelle deiner Daten und lege dann ihre Exportdatei auf der passenden Kachel ab.",
}

export default importPage
