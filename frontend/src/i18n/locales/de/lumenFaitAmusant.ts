import type fr from '../fr/lumenFaitAmusant'
import type { Structure } from '../../types'

/** Allemand — espace « lumenFaitAmusant » (backlog § BL.2), traduit depuis le français. */
const lumenFaitAmusant: Structure<typeof fr> = {
  unLumenCEstLe: "Ein Lumen ist der Lichtstrom einer Kerze in einem Meter Abstand. Ihr Vermögen hat keine SI-Einheit — den Namen behalten wir trotzdem.",
  fermer: "Schließen",
}

export default lumenFaitAmusant
