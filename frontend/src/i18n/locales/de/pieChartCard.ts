import type fr from '../fr/pieChartCard'
import type { Structure } from '../../types'

/** Allemand — espace « pieChartCard » (backlog § BL.2), traduit depuis le français. */
const pieChartCard: Structure<typeof fr> = {
  titreUniquePasDeDecomposition: "Einzelwertpapier, keine interne Aufteilung.",
}

export default pieChartCard
