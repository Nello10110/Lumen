import type fr from '../fr/compositionModal'
import type { Structure } from '../../types'

/** Allemand — espace « compositionModal » (backlog § BL.2), traduit depuis le français. */
const compositionModal: Structure<typeof fr> = {
  fermer: "Schließen",
  aucuneLigneNeComposeCette: "Keine Zeile gehört zu dieser Kategorie.",
  valeurTotale: "Gesamtwert:",
}

export default compositionModal
