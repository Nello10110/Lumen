import type fr from '../fr/holdingDetailModal'
import type { Structure } from '../../types'

/** Allemand — espace « holdingDetailModal » (backlog § BL.2), traduit depuis le français. */
const holdingDetailModal: Structure<typeof fr> = {
  fermer: "Schließen",
  ouvrirEnPleinePage: "Als ganze Seite öffnen",
}

export default holdingDetailModal
