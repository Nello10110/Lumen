import type fr from '../fr/pageIntrouvablePage'
import type { Structure } from '../../types'

/** Allemand — espace « pageIntrouvablePage » (backlog § BL.2), traduit depuis le français. */
const pageIntrouvablePage: Structure<typeof fr> = {
  aucuneLumiereParIci: "Hier ist kein Licht.",
  cettePageNExistePas: "Diese Seite existiert nicht.",
  retourAuTableauDeBord: "Zurück zur Übersicht",
}

export default pageIntrouvablePage
