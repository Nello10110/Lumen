/** Textes français — espace « pieChartCard » (backlog § BL.2). Généré par
 * `scripts/i18n-extraire.mjs`, puis relu à la main. */
const pieChartCard = {
  titreUniquePasDeDecomposition: "Titre unique, pas de décomposition interne.",
} as const

export default pieChartCard
