/** Textes français — espace « labelAdaptatif » (backlog § BL.2). Généré par
 * `scripts/i18n-extraire.mjs`, puis relu à la main. */
const labelAdaptatif = {
  langageSimple: "langage simple",
  termeTechnique: "terme technique",
} as const

export default labelAdaptatif
