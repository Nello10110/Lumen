/** Textes français — espace « compositionModal » (backlog § BL.2). Généré par
 * `scripts/i18n-extraire.mjs`, puis relu à la main. */
const compositionModal = {
  fermer: "Fermer",
  aucuneLigneNeComposeCette: "Aucune ligne ne compose cette catégorie.",
  valeurTotale: "Valeur totale :",
} as const

export default compositionModal
