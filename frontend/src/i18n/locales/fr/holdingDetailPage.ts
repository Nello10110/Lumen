/** Textes français — espace « holdingDetailPage » (backlog § BL.2). Généré par
 * `scripts/i18n-extraire.mjs`, puis relu à la main. */
const holdingDetailPage = {
  patrimoine: "Patrimoine",
} as const

export default holdingDetailPage
