/** Textes français — espace « holdingDetailModal » (backlog § BL.2). Généré par
 * `scripts/i18n-extraire.mjs`, puis relu à la main. */
const holdingDetailModal = {
  fermer: "Fermer",
  ouvrirEnPleinePage: "Ouvrir en pleine page",
} as const

export default holdingDetailModal
