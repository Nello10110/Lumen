/** Textes français — espace « compteDetailPage » (backlog § BL.2). Généré par
 * `scripts/i18n-extraire.mjs`, puis relu à la main. */
const compteDetailPage = {
  retourAuxComptes: "Retour aux comptes",
} as const

export default compteDetailPage
