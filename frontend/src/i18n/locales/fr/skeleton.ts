/** Textes français — espace « skeleton » (backlog § BL.2). Généré par
 * `scripts/i18n-extraire.mjs`, puis relu à la main. */
const skeleton = {
  chargementEnCours: "Chargement en cours",
} as const

export default skeleton
