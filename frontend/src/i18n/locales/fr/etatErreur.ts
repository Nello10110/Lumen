/** Textes français — espace « etatErreur » (backlog § BL.2). Généré par
 * `scripts/i18n-extraire.mjs`, puis relu à la main. */
const etatErreur = {
  reessayer: "Réessayer",
} as const

export default etatErreur
