-r requirements.txt
pytest>=8.0
pytest-cov>=5.0
httpx>=0.27
pypdf>=5.0
ruff>=0.16
# Pilote Postgres, pour la suite de tests sous Postgres (§ BI.4, job CI
# `backend-postgres`). Absent de requirements.txt : l'image n'en a pas l'usage tant
# qu'aucune installation ne tourne sur une base serveur.
psycopg[binary]>=3.2
